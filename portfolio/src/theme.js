import { createMuiTheme } from '@material-ui/core/';

const theme = createMuiTheme({
    palette: {
        primary: {
            main: '#eeeeee',
            light: '#ffffff',
            dark: '#bcbcbc',
            
        },
        secondary: {
            main: '#6a1b9a',
            light: '#9c4dcc',
            dark: '#38006b',
        }
    },
})

export default theme;